# Motion record - {ID}: {name}

A record stays **provisional** until every row is filled and tested. If the motion does not help the user understand a change, write "none" and keep the static UI.

| Field | Value |
| --- | --- |
| Page / component | |
| Why it exists | {what it explains: cause and effect, where something went, what changed} |
| Data prerequisite | {the real event it follows: confirmed add, loaded image, validated price} |
| Trigger | {click / focus / hover / meaningful visibility / data update}; once or repeat? |
| Start and end state | {both fully legible; content never waits for the animation} |
| Timeline | {duration role or ms, delay, distance/scale, easing role} |
| Loop policy | {one-shot / loops only while … ; at most one looping animation on screen} |
| Interrupt | {second click, fast repeat, route change, failed request, scroll back} |
| Keyboard and touch | {focus equivalent of hover; touch equivalent} |
| Reduced motion | {final state immediately; what still signals the change} |
| Screen reader | {what is announced, once} |
| Responsive / tall content | {phone behaviour; taller-than-viewport cards} |
| Performance | {transform/opacity only? will-change added and removed when done? low-power result} |
| Test result | {devices, date, pass/fail, notes} |
