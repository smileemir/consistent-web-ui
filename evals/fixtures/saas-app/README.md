# Tidewell

Team time tracking (fictional).

- `app/`: the web app frontend (static HTML, CSS and JavaScript)
- `docs/product-notes.md`: how the product behaves
- `server/`: the API, maintained by the backend team

Preview: run `python3 -m http.server` in this folder and open `/app/dashboard.html`.
