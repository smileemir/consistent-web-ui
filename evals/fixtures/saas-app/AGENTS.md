# Notes for AI assistants

- The frontend lives in `app/`. Preview with `python3 -m http.server` and open `/app/dashboard.html`.
- The backend team owns `server/`. Do not change it.
- Use 2-space indentation in HTML, CSS and JavaScript.
