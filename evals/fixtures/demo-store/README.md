# Kestrel Supply (fictional demo store)
Static frontend in the root folder; `server/` is the backend. Preview with `npm start` (or `python3 -m http.server 8080`) and open http://localhost:8080/index.html.
